name: Pull request

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  pull-request:
      uses: ./.github/workflows/tests.yml
      with:
        install_command: "python3 -m pip install -e ."
